# Copy this file to .env and fill in your Etherscan API key.
# Get a free key at https://etherscan.io/apis
# The key is read at build time by Vite — it is still bundled into the
# client JS, so treat it as a rate-limited public key, not a secret.
VITE_ETHERSCAN_API_KEY=YourApiKeyToken
